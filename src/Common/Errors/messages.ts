// import { CONSTANTS } from '../constants';

const ERRORS = {
  USER_EXISTS: {
    code: 1,
    error: 'USER_EXISTS_ERROR',
    message: 'User with this email already exists',
  },
};

export { ERRORS };
